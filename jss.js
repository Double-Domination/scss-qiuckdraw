// let element;
// console.log(typeof Infinity);

// let i = 0;
// while (i <= 3) {
//   console.log(i);
//   i++;
// }

let res = 7 * 3 + 4 / 2 - (8 + (2 - 1));

const some = (x, y) => (x > y ? x : y);

// console.log((0.32 * 0.22 * 0.75))

let dudos = 'metailen';
dudos.length = 50;

// console.log(dudos.length)
//console.log('Hey there!'

let arr = [
  0 === 1,
  1 === '1',
  1 == '1',
  4 === 1 + 3,
  'A' > 'a',
  'B' < 'b',
  '13' > 12,
  '13px' > 12,
  '13px' < '14',
];

const name = 'Vasia';

// console.log(1/0);

let a, b;
a = true;
b = true;

const mathingMap = [a && b, !a && b, a && !b, !a && !b, !(a && b), !(!a && !b)];

// console.table(mathingMap);
// console.log(
//   (!a) && (!b))

// console.log('after   _'+'_   before')

const precalc = '4' * '9';
// console.log(String.fromCharCode(59))
const assert1 = [0, '', {}, undefined, NaN, null, []];

// console.table(assert1.map(x=>!!x))

// console.log(!!{})
let assertI = 10;
while (assertI < 10) {
  console.log(assertI);
  assertI += 1;
}

const assert2 = [a || b, !a || b, a || !b, !a || !b, !(a || b), !(!a || !b)];
const assert2 = [a || b, !a || b, a || !b, !a || !b, !(a || b), !(!a || !b)];
const assert2 = [a || b, !a || b, a || !b, !a || !b, !(a || b), !(!a || !b)];
const assert2 = [a || b, !a || b, a || !b, !a || !b, !(a || b), !(!a || !b)];
const assert2 = [a || b, !a || b, a || !b, !a || !b, !(a || b), !(!a || !b)];

// console.table(assert2)

// console.log(Math.min(10, 222, -5, 0, '-33'))
